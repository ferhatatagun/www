name: Build and Deploy
on:
  push:
    branches:
      - master
permissions:
  contents: write
jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }} # Recommended if you intend to make multiple deployments in quick succession.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install and Build 🔧 # This example project is built using npm and outputs the result to the 'build' folder. Replace with the commands required to build your project, or remove this step entirely if your site is pre-built.
        env:
          # Prerendering every blog post via marked now does real SSR work
          # (markdown -> HTML) for ~14 routes. Default node heap (~1.4 GB on
          # ubuntu-latest workers) was OOMing during the prerender pass.
          NODE_OPTIONS: --max-old-space-size=4096
        run: |
          yarn install --immutable --immutable-cache --check-cache
          yarn build
          touch build/.nojekyll
          # SPA fallback for GitHub Pages deep links. We renamed the adapter
          # fallback to 200.html so it doesn't overwrite the prerendered home;
          # copy that to 404.html for GH Pages' deep-link fallback.
          cp build/200.html build/404.html

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: build # The folder the action should deploy.
          branch: gh-pages
